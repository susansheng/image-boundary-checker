# 部署到 Vercel 和 GitHub 指南

## 📋 部署步骤总览

1. 创建 GitHub 仓库并推送代码
2. 连接 Vercel 并自动部署
3. 配置模板图片（可选）

## 🚀 详细部署步骤

### 第一步：初始化 Git 仓库并推送到 GitHub

#### 1.1 初始化本地 Git 仓库

```bash
cd "/Users/sxsheng/Documents/代码/图片边界验收工具"

# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 创建第一个提交
git commit -m "Initial commit: 图片边界验收工具"
```

#### 1.2 创建 GitHub 仓库

方式一：使用 gh CLI（推荐）
```bash
# 创建公开仓库
gh repo create image-boundary-checker --public --source=. --push

# 或创建私有仓库
gh repo create image-boundary-checker --private --source=. --push
```

方式二：手动创建
1. 访问 https://github.com/new
2. 仓库名称：`image-boundary-checker`
3. 描述：`图片边界验收工具 - 自动检测图片是否符合边界规范`
4. 选择 Public 或 Private
5. 不要初始化 README（我们已经有了）
6. 点击 "Create repository"

#### 1.3 推送代码到 GitHub

如果是手动创建的仓库，执行：
```bash
# 添加远程仓库（替换为你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/image-boundary-checker.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 第二步：部署到 Vercel

#### 2.1 访问 Vercel

1. 打开浏览器访问：https://vercel.com
2. 使用 GitHub 账号登录

#### 2.2 导入项目

1. 点击 "Add New..." → "Project"
2. 从 GitHub 选择 `image-boundary-checker` 仓库
3. 点击 "Import"

#### 2.3 配置项目

**项目设置：**
- Framework Preset: `Other`
- Root Directory: `./`
- Build Command: （留空）
- Output Directory: （留空）
- Install Command: `pip install -r requirements.txt`

**环境变量：**（可选）
暂时不需要配置环境变量

#### 2.4 部署

1. 点击 "Deploy"
2. 等待部署完成（约 2-3 分钟）
3. 部署成功后会显示项目 URL

### 第三步：验证部署

1. 点击 Vercel 提供的 URL（类似：`https://image-boundary-checker.vercel.app`）
2. 测试上传图片功能
3. 检查是否正常显示红色边框叠加

## 📝 部署文件说明

项目包含以下部署相关文件：

### vercel.json
```json
{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
```

作用：配置 Vercel 构建和路由规则

### requirements.txt
```
Flask==3.0.0
Pillow==10.2.0
Werkzeug==3.0.1
```

作用：指定 Python 依赖包

### api/index.py
Flask 应用的 Vercel serverless 适配版本

### .gitignore
排除不需要提交的文件

## 🔧 注意事项

### 1. 模板图片

目前代码中模板图片路径是本地路径：
```python
TEMPLATE_PATH = "/Users/sxsheng/Downloads/模板.png"
```

**解决方案：**
1. 删除 `get_template()` 路由（已在 api/index.py 中移除）
2. 安全区域边界已硬编码在代码中
3. 如需在 Web 界面显示模板，可以将模板图片转为 base64 内嵌

### 2. 文件大小限制

Vercel Serverless 函数限制：
- 请求体大小：4.5MB（Hobby 计划）
- 函数执行时间：10 秒（Hobby 计划）

当前设置：
```python
app.config['MAX_CONTENT_LENGTH'] = 16 * 1024 * 1024
```

建议调整为 4MB：
```python
app.config['MAX_CONTENT_LENGTH'] = 4 * 1024 * 1024
```

### 3. 本地开发 vs 生产环境

**本地开发：**
```bash
cd "/Users/sxsheng/Documents/代码/图片边界验收工具"
python3 web_validator.py
```

**Vercel 生产环境：**
- 自动使用 `api/index.py`
- 无需手动启动服务器
- 每次推送到 GitHub 自动部署

## 🔄 更新部署

每次修改代码后：

```bash
# 提交更改
git add .
git commit -m "更新说明"

# 推送到 GitHub
git push

# Vercel 会自动检测并重新部署
```

## 📊 部署状态检查

### 查看部署日志

1. 访问 Vercel Dashboard
2. 选择项目 `image-boundary-checker`
3. 点击 "Deployments"
4. 查看每次部署的日志和状态

### 常见问题

**问题 1：部署失败 - ModuleNotFoundError**

解决：检查 `requirements.txt` 是否包含所有依赖

**问题 2：404 错误**

解决：检查 `vercel.json` 路由配置是否正确

**问题 3：图片上传失败**

解决：检查文件大小是否超过限制

## 🌐 自定义域名（可选）

在 Vercel 项目设置中：
1. 进入 "Settings" → "Domains"
2. 添加自定义域名
3. 按照提示配置 DNS 记录

## 📞 获取帮助

- Vercel 文档：https://vercel.com/docs
- GitHub 文档：https://docs.github.com

## ✅ 部署检查清单

- [ ] 代码已推送到 GitHub
- [ ] Vercel 项目已创建
- [ ] 部署成功
- [ ] 测试上传功能
- [ ] 测试边框叠加功能
- [ ] 测试不同尺寸图片自动缩放
- [ ] 检查错误提示是否正常

---

**部署完成后，你的工具就可以在线访问了！** 🎉
